table:
  name: address
  schema: public
object_relationships:
  - name: person
    using:
      manual_configuration:
        column_mapping:
          person_id: id
        insertion_order: null
        remote_table:
          name: person
          schema: public
select_permissions:
  - role: address-reader
    permission:
      columns:
        - address_lines
        - administrative_area
        - locality
        - postal_code
        - region_code
        - sublocality
        - id
        - person_id
      filter: {}
